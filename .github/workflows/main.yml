name: 📡 IRAN NOW - Ultimate Manual Stream
on:
  workflow_dispatch:
    inputs:
      url1: { description: 'رابط فيديو 1', required: true }
      url2: { description: 'رابط فيديو 2', required: true }
      url3: { description: 'رابط فيديو 3', required: true }
      url4: { description: 'رابط فيديو 4', required: true }
      url5: { description: 'رابط فيديو 5', required: true }
      bottom_text: { description: 'نص الشريط', required: false, default: 'بث مباشر 24/7' }
      stream_duration: { description: 'المدة بالساعات', required: false, default: '5' }

env:
  STREAM_KEY: ${{ secrets.YOUTUBE_STREAM_KEY }}

jobs:
  stream:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: تثبيت المتطلبات الكاملة مع دعم impersonation
      run: |
        sudo apt-get update
        sudo apt-get install -y ffmpeg fonts-dejavu-core python3-pip python3-venv
        
        # تثبيت مكتبات النظام الضرورية للمحاكاة
        sudo apt-get install -y \
          curl \
          wget \
          libcurl4-openssl-dev \
          libssl-dev \
          python3-dev \
          build-essential \
          pkg-config \
          libffi-dev \
          libxml2-dev \
          libxslt1-dev
        
        # تحديث pip وتثبيت curl-cffi أولاً
        pip3 install --upgrade pip
        pip3 install --upgrade setuptools wheel
        
        # تثبيت curl-cffi للمحاكاة المتقدمة
        pip3 install --upgrade curl-cffi
        
        # تثبيت yt-dlp مع جميع الإضافات
        pip3 install --upgrade "yt-dlp[default,curl-cffi,impersonate]" brotli certifi gdown
        
        # عرض إصدار yt-dlp وأهداف المحاكاة المتاحة
        echo "✅ yt-dlp version: $(yt-dlp --version)"
        echo "✅ Available impersonation targets:"
        yt-dlp --list-impersonate-targets || echo "⚠️ Impersonation targets not available yet"

    - name: تجهيز الخلفية والتوقيت
      id: timing
      run: |
        gdown --id 1mcqB-MvngWziGqNPuRs9gO39XtdbddgJ -O background.png || ffmpeg -f lavfi -i color=c=black:s=1920x1080 -frames:v 1 background.png
        echo "end_time=$(($(date +%s) + (${{ github.event.inputs.stream_duration }} * 3600)))" >> $GITHUB_OUTPUT

    - name: بدء البث المباشر
      run: |
        # دالة استخراج الروابط مع محاولات متعددة للمحاكاة
        extract_url() {
          echo "🔄 محاولة استخراج: $1" >&2
          
          # المحاولة 1: impersonate chrome مع curl-cffi
          yt-dlp --quiet --no-warnings --get-url \
            --impersonate chrome:windows-10 \
            --extractor-args "dailymotion:impersonate=firefox" \
            "$1" 2>/dev/null && echo "✅ نجح مع chrome impersonation" >&2 && return
          
          # المحاولة 2: impersonate firefox
          yt-dlp --quiet --no-warnings --get-url \
            --impersonate firefox:windows-10 \
            "$1" 2>/dev/null && echo "✅ نجح مع firefox impersonation" >&2 && return
          
          # المحاولة 3: impersonate safari
          yt-dlp --quiet --no-warnings --get-url \
            --impersonate safari:macos-11 \
            "$1" 2>/dev/null && echo "✅ نجح مع safari impersonation" >&2 && return
          
          # المحاولة 4: بدون impersonation مع headers مخصصة
          yt-dlp --quiet --no-warnings --get-url \
            --user-agent "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36" \
            --add-header "Accept: text/html,application/xhtml+xml,application/xml;q=0.9,image/webp,*/*;q=0.8" \
            --add-header "Accept-Language: en-US,en;q=0.5" \
            --add-header "Referer: https://www.dailymotion.com/" \
            --add-header "DNT: 1" \
            "$1" 2>/dev/null && echo "✅ نجح مع headers مخصصة" >&2 && return
          
          # المحاولة 5: استخدام ffmpeg مباشرة (كحل أخير)
          echo "⚠️ فشلت جميع محاولات yt-dlp، سيتم استخدام الرابط الأصلي" >&2
          echo "$1"
        }

        # استخراج جميع الروابط
        echo "🔄 جاري استخراج روابط الفيديو..."
        V1=$(extract_url "${{ github.event.inputs.url1 }}")
        V2=$(extract_url "${{ github.event.inputs.url2 }}")
        V3=$(extract_url "${{ github.event.inputs.url3 }}")
        V4=$(extract_url "${{ github.event.inputs.url4 }}")
        V5=$(extract_url "${{ github.event.inputs.url5 }}")
        
        echo "✅ تم استخراج الروابط بنجاح"

        # فلتر دمج الشاشات مع معالجة حجم الخلفية
        FILTER="[1:v]scale=960:540,setsar=1[v1]; [2:v]scale=960:540,setsar=1[v2]; \
                [3:v]scale=640:540,setsar=1[v3]; [4:v]scale=640:540,setsar=1[v4]; \
                [5:v]scale=640:540,setsar=1[v5]; [0:v]scale=1920:1080[bg]; \
                [bg][v1]overlay=0:0[b1]; [b1][v2]overlay=960:0[b2]; \
                [b2][v3]overlay=0:540[b3]; [b3][v4]overlay=640:540[b4]; \
                [b4][v5]overlay=1280:540[b5]; \
                [b5]drawtext=text='${{ github.event.inputs.bottom_text }}':fontfile=/usr/share/fonts/truetype/dejavu/DejaVuSans-Bold.ttf:fontcolor=white:fontsize=60:x=w-mod(150*t\,w+tw):y=h-80:box=1:boxcolor=black@0.6[out]"

        # بدء البث مع إعادة المحاولة تلقائياً
        echo "🔄 بدء البث المباشر حتى $(date -d @${{ steps.timing.outputs.end_time }})"
        
        while [ $(date +%s) -lt ${{ steps.timing.outputs.end_time }} ]; do
          echo "🚀 بدء دورة بث جديدة في $(date)"
          
          ffmpeg -re -stream_loop -1 -i background.png \
            -reconnect 1 -reconnect_at_eof 1 -reconnect_streamed 1 -reconnect_delay_max 5 \
            -timeout 30000000 \
            -rw_timeout 30000000 \
            -i "$V1" -i "$V2" -i "$V3" -i "$V4" -i "$V5" \
            -filter_complex "$FILTER" -map "[out]" -map 1:a \
            -c:v libx264 -preset ultrafast -tune zerolatency -b:v 2500k -maxrate 2500k -bufsize 5000k \
            -pix_fmt yuv420p -g 60 -c:a aac -b:a 128k -ar 44100 \
            -f flv "rtmp://a.rtmp.youtube.com/live2/$STREAM_KEY"
          
          # إذا انتهى البث بسبب خطأ، انتظر قليلاً وحاول مرة أخرى
          echo "⚠️ توقف البث، إعادة المحاولة بعد 10 ثواني..."
          sleep 10
          
          # إعادة استخراج الروابط إذا فشلت (بعض الروابط قد تنتهي صلاحيتها)
          if [ $? -ne 0 ]; then
            echo "🔄 إعادة استخراج الروابط..."
            V1=$(extract_url "${{ github.event.inputs.url1 }}")
            V2=$(extract_url "${{ github.event.inputs.url2 }}")
            V3=$(extract_url "${{ github.event.inputs.url3 }}")
            V4=$(extract_url "${{ github.event.inputs.url4 }}")
            V5=$(extract_url "${{ github.event.inputs.url5 }}")
          fi
        done
        
        echo "✅ انتهى وقت البث المقرر"
