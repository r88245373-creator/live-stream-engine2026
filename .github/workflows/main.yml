name: 📡 IRAN NOW - Ultimate Manual Stream
on:
  workflow_dispatch:
    inputs:
      url1: { description: 'رابط فيديو 1', required: true }
      url2: { description: 'رابط فيديو 2', required: true }
      url3: { description: 'رابط فيديو 3', required: true }
      url4: { description: 'رابط فيديو 4', required: true }
      url5: { description: 'رابط فيديو 5', required: true }
      bottom_text: { description: 'نص الشريط', required: false, default: 'بث مباشر 24/7' }
      stream_duration: { description: 'المدة بالساعات', required: false, default: '5' }

env:
  STREAM_KEY: ${{ secrets.YOUTUBE_STREAM_KEY }}

jobs:
  stream:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: تثبيت المتطلبات الكاملة
      run: |
        sudo apt-get update
        sudo apt-get install -y ffmpeg fonts-dejavu-core python3-pip python3-venv
        
        # تثبيت مكتبات النظام
        sudo apt-get install -y \
          curl \
          wget \
          libcurl4-openssl-dev \
          libssl-dev \
          python3-dev \
          build-essential \
          pkg-config
        
        # تحديث pip وتثبيت الأدوات
        pip3 install --upgrade pip
        pip3 install --upgrade curl-cffi
        pip3 install --upgrade "yt-dlp[default,curl-cffi]" brotli certifi gdown
        
        # إنشاء فيديو احتياطي في حالة فشل أي رابط
        ffmpeg -f lavfi -i testsrc=size=640x480:rate=30:duration=300 \
               -f lavfi -i sine=frequency=1000:duration=300 \
               -c:v libx264 -c:a aac -t 300 placeholder.mp4

    - name: تجهيز الخلفية والتوقيت
      id: timing
      run: |
        gdown --id 1mcqB-MvngWziGqNPuRs9gO39XtdbddgJ -O background.png || ffmpeg -f lavfi -i color=c=black:s=1920x1080 -frames:v 1 background.png
        echo "end_time=$(($(date +%s) + (${{ github.event.inputs.stream_duration }} * 3600)))" >> $GITHUB_OUTPUT

    - name: بدء البث المباشر
      run: |
        # دالة استخراج الروابط مع محاولات متعددة
        extract_url() {
          local url="$1"
          local output=""
          
          echo "🔄 محاولة استخراج: $url" >&2
          
          # محاولة مع yt-dlp وجميع خيارات المحاكاة
          for impersonate in "chrome:windows-10" "firefox:windows-10" "safari:macos-11"; do
            output=$(yt-dlp --quiet --no-warnings --get-url \
              --impersonate "$impersonate" \
              --extractor-args "generic:impersonate=$impersonate" \
              --user-agent "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36" \
              --referer "https://www.google.com/" \
              "$url" 2>/dev/null | head -1) && {
              echo "✅ نجح مع impersonate: $impersonate" >&2
              echo "$output"
              return 0
            }
          done
          
          # محاولة مع خيارات إضافية للمواقع المباشرة
          output=$(yt-dlp --quiet --no-warnings --get-url \
            --user-agent "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36" \
            --add-header "Accept: text/html,application/xhtml+xml,application/xml;q=0.9,image/webp,*/*;q=0.8" \
            --add-header "Accept-Language: en-US,en;q=0.5" \
            --add-header "Referer: https://www.google.com/" \
            --add-header "Origin: https://www.google.com" \
            --add-header "DNT: 1" \
            --add-header "Connection: keep-alive" \
            --add-header "Upgrade-Insecure-Requests: 1" \
            --extractor-args "generic:no-playlist" \
            --force-ipv4 \
            "$url" 2>/dev/null | head -1) && {
            echo "✅ نجح مع headers مخصصة" >&2
            echo "$output"
            return 0
          }
          
          # فشلت جميع المحاولات
          echo "⚠️ فشلت جميع المحاولات لـ $url" >&2
          echo "placeholder"
          return 1
        }

        # استخراج جميع الروابط
        echo "🔄 جاري استخراج روابط الفيديو..."
        
        URL1="${{ github.event.inputs.url1 }}"
        URL2="${{ github.event.inputs.url2 }}"
        URL3="${{ github.event.inputs.url3 }}"
        URL4="${{ github.event.inputs.url4 }}"
        URL5="${{ github.event.inputs.url5 }}"
        
        # مصفوفة لتخزين الروابط
        declare -a VIDEO_URLS
        declare -a ORIGINAL_URLS
        
        # استخراج كل رابط
        for i in {1..5}; do
          url_var="URL$i"
          original_url="${!url_var}"
          ORIGINAL_URLS[$i]="$original_url"
          
          extracted=$(extract_url "$original_url")
          VIDEO_URLS[$i]="$extracted"
          
          echo "V$i: ${VIDEO_URLS[$i]}"
        done

        # فلتر دمج الشاشات
        FILTER="[1:v]scale=960:540,setsar=1[v1]; [2:v]scale=960:540,setsar=1[v2]; \
                [3:v]scale=640:540,setsar=1[v3]; [4:v]scale=640:540,setsar=1[v4]; \
                [5:v]scale=640:540,setsar=1[v5]; [0:v]scale=1920:1080[bg]; \
                [bg][v1]overlay=0:0[b1]; [b1][v2]overlay=960:0[b2]; \
                [b2][v3]overlay=0:540[b3]; [b3][v4]overlay=640:540[b4]; \
                [b4][v5]overlay=1280:540[b5]; \
                [b5]drawtext=text='${{ github.event.inputs.bottom_text }}':fontfile=/usr/share/fonts/truetype/dejavu/DejaVuSans-Bold.ttf:fontcolor=white:fontsize=60:x=w-mod(150*t\,w+tw):y=h-80:box=1:boxcolor=black@0.6[out]"

        # بدء البث مع إعادة المحاولة
        echo "🔄 بدء البث المباشر حتى $(date -d @${{ steps.timing.outputs.end_time }})"
        
        while [ $(date +%s) -lt ${{ steps.timing.outputs.end_time }} ]; do
          echo "🚀 بدء دورة بث جديدة في $(date)"
          
          # تجهيز مدخلات ffmpeg مع التحقق من صحة كل رابط
          FFMPEG_INPUTS=""
          for i in {1..5}; do
            url="${VIDEO_URLS[$i]}"
            if [[ "$url" == "placeholder" ]]; then
              FFMPEG_INPUTS="$FFMPEG_INPUTS -stream_loop -1 -i placeholder.mp4"
            else
              FFMPEG_INPUTS="$FFMPEG_INPUTS -reconnect 1 -reconnect_at_eof 1 -reconnect_streamed 1 -reconnect_delay_max 5 -timeout 30000000 -rw_timeout 30000000 -i \"$url\""
            fi
          done
          
          # بناء وتنفيذ أمر ffmpeg
          eval ffmpeg -re -stream_loop -1 -i background.png \
            $FFMPEG_INPUTS \
            -filter_complex "$FILTER" -map "[out]" -map 1:a \
            -c:v libx264 -preset ultrafast -tune zerolatency -b:v 2500k -maxrate 2500k -bufsize 5000k \
            -pix_fmt yuv420p -g 60 -c:a aac -b:a 128k -ar 44100 \
            -f flv "rtmp://a.rtmp.youtube.com/live2/$STREAM_KEY"
          
          # إذا فشل البث، انتظر وحاول مرة أخرى
          echo "⚠️ توقف البث، إعادة المحاولة بعد 10 ثواني..."
          sleep 10
          
          # إعادة استخراج الروابط التي فشلت فقط
          echo "🔄 إعادة استخراج الروابط الفاشلة..."
          for i in {1..5}; do
            if [[ "${VIDEO_URLS[$i]}" == "placeholder" ]] || [[ $? -ne 0 ]]; then
              new_url=$(extract_url "${ORIGINAL_URLS[$i]}")
              VIDEO_URLS[$i]="$new_url"
              echo "V$i محدث: ${VIDEO_URLS[$i]}"
            fi
          done
        done
        
        echo "✅ انتهى وقت البث المقرر"
