name: 📡 IRAN NOW - Professional 24/7 Live Stream Engine
on:
  schedule:
    - cron: '55 */5 * * *'
  workflow_dispatch:
    inputs:
      bottom_text:
        description: 'نص الشريط المتحرك السفلي'
        required: false
        default: 'بث مباشر 24/7'
      stream_duration:
        description: 'مدة البث بالساعات'
        required: false
        default: '5'

env:
  STREAM_KEY: ${{ secrets.YOUTUBE_STREAM_KEY }}
  FFMPEG_LOG_LEVEL: warning

jobs:
  stream:
    name: تشغيل البث المباشر
    runs-on: ubuntu-latest
    timeout-minutes: 350 # زيادة المدة لتغطية الـ 5 ساعات والاستراحة

    steps:
    - name: استنساخ المستودع
      uses: actions/checkout@v3

    - name: تثبيت FFmpeg والإضافات
      run: |
        sudo apt-get update
        sudo apt-get install -y ffmpeg fonts-dejavu-core bc

    - name: التحقق من الملفات والروابط
      run: |
        for i in {1..5}; do
          if [ ! -f "L$i.txt" ]; then echo "❌ L$i.txt missing"; exit 1; fi
        done
        if [ ! -f "background.png" ]; then
          echo "⚠️ background.png غير موجود، سيتم إنشاء خلفية سوداء"
          ffmpeg -f lavfi -i color=c=black:s=1920x1080 -frames:v 1 background.png
        fi

    - name: حساب التوقيت
      id: timing
      run: |
        START_TIME=$(date +%s)
        DURATION_HOURS=${{ github.event.inputs.stream_duration || 5 }}
        END_TIME=$(($START_TIME + ($DURATION_HOURS * 3600)))
        echo "start_time=$START_TIME" >> $GITHUB_OUTPUT
        echo "end_time=$END_TIME" >> $GITHUB_OUTPUT

    - name: بدء البث الاحترافي
      run: |
        # قراءة الروابط مع تنظيف المسافات
        URL1=$(cat L1.txt | tr -d '\r\n')
        URL2=$(cat L2.txt | tr -d '\r\n')
        URL3=$(cat L3.txt | tr -d '\r\n')
        URL4=$(cat L4.txt | tr -d '\r\n')
        URL5=$(cat L5.txt | tr -d '\r\n')

        # فلتر توزيع الشاشات (توزيع 2 فوق و 3 تحت) لـ 1080p
        # الشاشات العلوية: 960x540 | الشاشات السفلية: 640x540
        FILTER="[1:v]scale=960:540,setsar=1[v1]; \
                [2:v]scale=960:540,setsar=1[v2]; \
                [3:v]scale=640:540,setsar=1[v3]; \
                [4:v]scale=640:540,setsar=1[v4]; \
                [5:v]scale=640:540,setsar=1[v5]; \
                [0:v][v1]overlay=0:0[b1]; \
                [b1][v2]overlay=960:0[b2]; \
                [b2][v3]overlay=0:540[b3]; \
                [b3][v4]overlay=640:540[b4]; \
                [b4][v5]overlay=1280:540[b5]; \
                [b5]drawtext=text='${{ github.event.inputs.bottom_text || 'بث مباشر 24/7' }}':\
                fontfile=/usr/share/fonts/truetype/dejavu/DejaVuSans-Bold.ttf:fontcolor=white:fontsize=60:\
                x=w-mod(150*t\,w+tw):y=h-80:box=1:boxcolor=black@0.6[out]"

        while [ $(date +%s) -lt ${{ steps.timing.outputs.end_time }} ]; do
          echo "🔄 تشغيل دورة البث..."
          ffmpeg -re -stream_loop -1 -i background.png \
            -reconnect 1 -reconnect_streamed 1 -reconnect_delay_max 5 \
            -i "$URL1" -i "$URL2" -i "$URL3" -i "$URL4" -i "$URL5" \
            -filter_complex "$FILTER" -map "[out]" \
            -c:v libx264 -preset ultrafast -tune zerolatency -b:v 3500k -maxrate 3500k -bufsize 7000k \
            -pix_fmt yuv420p -g 60 -c:a aac -b:a 128k -ar 44100 \
            -f flv "rtmp://a.rtmp.youtube.com/live2/$STREAM_KEY" || echo "⚠️ تعثر الاتصال، إعادة محاولة..."
          sleep 5
        done
