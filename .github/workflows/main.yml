name: 📡 IRAN NOW - Manual Input Live Stream
on:
  workflow_dispatch:
    inputs:
      url1:
        description: 'رابط الفيديو الأول (الرئيسي للصوت)'
        required: true
      url2:
        description: 'رابط الفيديو الثاني'
        required: true
      url3:
        description: 'رابط الفيديو الثالث'
        required: true
      url4:
        description: 'رابط الفيديو الرابع'
        required: true
      url5:
        description: 'رابط الفيديو الخامس'
        required: true
      bottom_text:
        description: 'نص الشريط المتحرك'
        required: false
        default: 'بث مباشر 24/7'
      stream_duration:
        description: 'مدة البث بالساعات'
        required: false
        default: '5'

env:
  STREAM_KEY: ${{ secrets.YOUTUBE_STREAM_KEY }}
  FFMPEG_LOG_LEVEL: warning

jobs:
  stream:
    name: تشغيل البث المباشر
    runs-on: ubuntu-latest
    timeout-minutes: 350

    steps:
    - name: استنساخ المستودع
      uses: actions/checkout@v4

    - name: تثبيت FFmpeg
      run: |
        sudo apt-get update
        sudo apt-get install -y ffmpeg fonts-dejavu-core bc

    - name: حساب وقت النهاية
      id: timing
      run: |
        START_TIME=$(date +%s)
        DURATION=${{ github.event.inputs.stream_duration || 5 }}
        END_TIME=$(($START_TIME + ($DURATION * 3600)))
        echo "end_time=$END_TIME" >> $GITHUB_OUTPUT

    - name: بدء البث من المدخلات اليدوية
      run: |
        echo "🚀 بدء البث باستخدام الروابط المدخلة يدوياً..."

        # تعريف الفلتر لتقسيم الشاشة 1080p (2 فوق و 3 تحت)
        FILTER="[1:v]scale=960:540,setsar=1[v1]; \
                [2:v]scale=960:540,setsar=1[v2]; \
                [3:v]scale=640:540,setsar=1[v3]; \
                [4:v]scale=640:540,setsar=1[v4]; \
                [5:v]scale=640:540,setsar=1[v5]; \
                [0:v][v1]overlay=0:0[b1]; \
                [b1][v2]overlay=960:0[b2]; \
                [b2][v3]overlay=0:540[b3]; \
                [b3][v4]overlay=640:540[b4]; \
                [b4][v5]overlay=1280:540[b5]; \
                [b5]drawtext=text='${{ github.event.inputs.bottom_text }}':\
                fontfile=/usr/share/fonts/truetype/dejavu/DejaVuSans-Bold.ttf:fontcolor=white:fontsize=60:\
                x=w-mod(150*t\,w+tw):y=h-80:box=1:boxcolor=black@0.6[out]"

        # حلقة البث المستمر
        while [ $(date +%s) -lt ${{ steps.timing.outputs.end_time }} ]; do
          ffmpeg -re -stream_loop -1 -i background.png \
            -reconnect 1 -reconnect_at_eof 1 -reconnect_streamed 1 -reconnect_delay_max 5 \
            -i "${{ github.event.inputs.url1 }}" \
            -i "${{ github.event.inputs.url2 }}" \
            -i "${{ github.event.inputs.url3 }}" \
            -i "${{ github.event.inputs.url4 }}" \
            -i "${{ github.event.inputs.url5 }}" \
            -filter_complex "$FILTER" -map "[out]" -map 1:a \
            -c:v libx264 -preset ultrafast -tune zerolatency -b:v 3000k -maxrate 3000k -bufsize 6000k \
            -pix_fmt yuv420p -g 60 -c:a aac -b:a 128k -ar 44100 \
            -f flv "rtmp://a.rtmp.youtube.com/live2/$STREAM_KEY" || echo "⚠️ محاولة إعادة اتصال..."
          sleep 5
        done
